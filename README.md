# RandomForestClassifier-created-with-DecisionTreeClassifiers
Build random forest from 1000 decision trees  Trained each tree on a random 100-instance subset (ShuffleSplit) and combine predictions by majority vote with scipy.stats.
